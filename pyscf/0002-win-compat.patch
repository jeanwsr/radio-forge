diff --git a/MANIFEST.in b/MANIFEST.in
index 469d803cf..97b9bc8fa 100644
--- a/MANIFEST.in
+++ b/MANIFEST.in
@@ -11,6 +11,13 @@ include pyscf/lib/deps/lib*/libcint.[4-9].dylib
 include pyscf/lib/deps/lib*/libxc.*.dylib
 include pyscf/lib/deps/lib*/libxcfun.[2-9].dylib
 
+# windows dynamic libraries
+include pyscf/lib/*.dll
+include pyscf/lib/deps/bin/*.dll
+include pyscf/lib/deps/lib*/libcint.dll
+include pyscf/lib/deps/lib*/libxc.dll
+include pyscf/lib/deps/lib*/libxcfun.dll
+
 include pyscf/geomopt/log.ini
 include pyscf/gto/basis/bse_meta.json
 
diff --git a/examples/2-benchmark/benchmarking_utils.py b/examples/2-benchmark/benchmarking_utils.py
index 78e3c37ea..7d9882a23 100644
--- a/examples/2-benchmark/benchmarking_utils.py
+++ b/examples/2-benchmark/benchmarking_utils.py
@@ -1,16 +1,27 @@
 import os
+import sys
 import pyscf
 from pyscf.lib.logger import perf_counter, process_clock
 
 def setup_logger():
     log = pyscf.lib.logger.Logger(verbose=5)
-    with open('/proc/cpuinfo') as f:
-        for line in f:
-            if 'model name' in line:
-                log.note(line[:-1])
-                break
-    with open('/proc/meminfo') as f:
-        log.note(f.readline()[:-1])
+    if sys.platform.startswith('linux'):
+        with open('/proc/cpuinfo') as f:
+            for line in f:
+                if 'model name' in line:
+                    log.note(line[:-1])
+                    break
+        with open('/proc/meminfo') as f:
+            log.note(f.readline()[:-1])
+    else:
+        try:
+            import psutil
+            log.note('CPU: %s', psutil.cpu_freq())
+            mem = psutil.virtual_memory()
+            log.note('Memory: %d MB total', mem.total / (1024 * 1024))
+        except (ImportError, Exception):
+            import platform
+            log.note('Platform: %s', platform.platform())
     log.note('OMP_NUM_THREADS=%s\n', os.environ.get('OMP_NUM_THREADS', None))
     return log
 
diff --git a/pyscf/dft/numint.py b/pyscf/dft/numint.py
index dfb2ef26a..c75831ea3 100644
--- a/pyscf/dft/numint.py
+++ b/pyscf/dft/numint.py
@@ -22,6 +22,7 @@ Numerical integration functions for RKS and UKS with real AO basis
 
 import warnings
 import ctypes
+import sys
 import numpy
 from pyscf import lib
 try:
@@ -39,6 +40,11 @@ from pyscf.dft import xc_deriv
 from pyscf import __config__
 
 libdft = lib.load_library('libdft')
+if sys.platform == 'win32':
+    libcvhf = lib.load_library('libcvhf')
+    libcgto = lib.load_library('libcgto')
+    libcint = lib.load_library('libcint')
+    libdft = lib.make_dll_wrapper(libdft, libcvhf, libcgto, libcint)
 OCCDROP = getattr(__config__, 'dft_numint_occdrop', 1e-12)
 # The system size above which to consider the sparsity of the density matrix.
 # If the number of AOs in the system is less than this value, all tensors are
diff --git a/pyscf/lib/misc.py b/pyscf/lib/misc.py
index 746e8583e..e12243705 100644
--- a/pyscf/lib/misc.py
+++ b/pyscf/lib/misc.py
@@ -97,6 +97,13 @@ def load_library(libname):
         _loaderpath = os.path.dirname(__file__)
         return numpy.ctypeslib.load_library(libname, _loaderpath)
     except OSError:
+        if sys.platform == 'win32':
+            for env_path in [os.path.join(sys.prefix, 'Library', 'bin'),
+                             os.path.join(sys.prefix, 'Library', 'lib')]:
+                try:
+                    return numpy.ctypeslib.load_library(libname, env_path)
+                except OSError:
+                    pass
         from pyscf import __path__ as ext_modules
         for path in ext_modules:
             libpath = os.path.join(path, 'lib')
@@ -106,20 +113,39 @@ def load_library(libname):
                         return numpy.ctypeslib.load_library(libname, libpath)
         raise
 
+
+def make_dll_wrapper(lib, *fallbacks):
+    if sys.platform != 'win32':
+        return lib
+    class _DllWrapper:
+        def __init__(self, primary, *fallbacks):
+            object.__setattr__(self, '_primary', primary)
+            object.__setattr__(self, '_fallbacks', fallbacks)
+        def __getattr__(self, name):
+            for dll in (self._primary,) + self._fallbacks:
+                try:
+                    return getattr(dll, name)
+                except AttributeError:
+                    pass
+            raise AttributeError(f"function '{name}' not found")
+    return _DllWrapper(lib, *fallbacks)
+
 #Fixme, the standard resource module gives wrong number when objects are released
 # http://fa.bianp.net/blog/2013/different-ways-to-get-memory-consumption-or-lessons-learned-from-memory_profiler/#fn:1
 #or use slow functions as memory_profiler._get_memory did
-CLOCK_TICKS = os.sysconf("SC_CLK_TCK")
-PAGESIZE = os.sysconf("SC_PAGE_SIZE")
 def current_memory():
     '''Return the size of used memory and allocated virtual memory (in MB)'''
-    #import resource
-    #return resource.getrusage(resource.RUSAGE_SELF).ru_maxrss / 1000
-    if sys.platform.startswith('linux'):
-        with open("/proc/%s/statm" % os.getpid()) as f:
-            vms, rss = [int(x)*PAGESIZE for x in f.readline().split()[:2]]
-            return rss/1e6, vms/1e6
-    else:
+    try:
+        import psutil
+        process = psutil.Process(os.getpid())
+        mem_info = process.memory_info()
+        rss = mem_info.rss
+        vms = mem_info.vms
+        return rss/1e6, vms/1e6
+    except (ImportError, Exception):
+        if sys.platform.startswith('linux'):
+            import resource
+            return resource.getrusage(resource.RUSAGE_SELF).ru_maxrss / 1e6, 0
         return 0, 0
 
 def num_threads(n=None):
diff --git a/setup.py b/setup.py
index 67ed61cba..3cbe6a551 100755
--- a/setup.py
+++ b/setup.py
@@ -61,6 +61,9 @@ class CMakeBuildPy(build_py):
         self.announce('Configuring extensions', level=3)
         src_dir = os.path.abspath(os.path.join(__file__, '..', 'pyscf', 'lib'))
         cmd = ['cmake', f'-S{src_dir}', f'-B{self.build_temp}']
+        # On Windows, DLLs go to CMAKE_BINARY_DIR by default. Route them to pyscf/lib
+        # alongside .so/.dylib on other platforms.
+        cmd.append(f'-DCMAKE_RUNTIME_OUTPUT_DIRECTORY={src_dir}')
         configure_args = os.getenv('CMAKE_CONFIGURE_ARGS')
         if configure_args:
             cmd.extend(configure_args.split(' '))
